// global
const GLOBAL_MAX_WIDTH = 768;
const GLOBAL_PADDING_X = 8;

// global header
const GNB_HEIGHT = 80;

// main
const MAIN_MARGIN_TOP = GNB_HEIGHT + 8;

export default {
  GLOBAL_MAX_WIDTH,
  GLOBAL_PADDING_X,

  GNB_HEIGHT,

  MAIN_MARGIN_TOP,
};
